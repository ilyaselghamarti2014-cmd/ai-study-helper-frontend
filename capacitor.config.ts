import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aistudyhelper.app',
  appName: 'AI Study Helper',
  webDir: 'public'
};

export default config;
